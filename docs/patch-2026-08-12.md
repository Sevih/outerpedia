# Patch du 2026-08-12

> Live après maintenance, 12/08 06:00 UTC. Supprimer ce fichier dans le commit
> qui le solde.

## Données

- [ ] `pnpm dev`, puis lire le diff : Demiurge Saeran + son EE _Original Sin_,
      `equipment/enhance.json` → `singularity` (`rate` → 100, coûts revus),
      saison Joint Challenge dans `content-schedule.json`

## Demiurge Saeran

- [ ] `/admin/editor/characters/<id>` — `role`, `skillPriority`, `videos`, reco
      d'équipement, puis `rank` / `rankPvp`
- [ ] `/admin/editor/ee/<id>` — `rank` + `rank10`
- [ ] si le build warn sur un `BT_` inédit : le classer dans `effect-families.json`

## Ascension Singularity — les taux de réussite disparaissent

- [ ] `src/components/equipment/EquipmentDetail.tsx` — retirer la colonne `%`
      de `AxStepsCard`, supprimer `axRateColor`
- [ ] `equip.detail.step_rates` → « coûts par palier », 5 locales
- [ ] `general-guides/gear/labels.ts` — les deux entrées « may fail / success
      rate decreases » ; vérifier `index.tsx` (`rules.singularity`)

## Joint Challenge — Prototype EX-78

- [ ] `pnpm datagen:version-boss` → `versions/2026-08/`
- [ ] réviser `config.json` / `recommended.json` / `teams.json` / `tips.json`
      depuis `2026-03`, puis `meta.json.updated`

## Singularité — 2 entrées quotidiennes → 1

- [ ] `progress-tracker/tasks.ts` — `'dimensional-singularity'` : `maxCount`
      2 → 1, et le commentaire au-dessus
- [ ] `timegate-resources.json` — recompter en jeu ; ne rien modifier si les
      totaux tombent juste (les récompenses des 2 entrées sont fusionnées)

## Publication

- [ ] `pnpm commit`, DONE.md dans le même commit

---

Écarté : recrutement Demiurge (premium, permanente) · event dungeon et son shop
· packages et Battle Pass (argent réel) · Chat, Tactics League, écran Aventure ·
damage calculator (non publié).
