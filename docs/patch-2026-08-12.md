# Patch du 2026-08-12

> Live depuis la maintenance (06:00 UTC). Supprimer ce fichier dans le commit
> qui le solde.

## Données

- [x] `pnpm dev` — Saeran est extrait sous l'id **2000129** (Dark, Ranger)
- [ ] `pnpm datagen:promote --apply` — le lot est encore en proposition, dont
      `equipment/enhance.json` (les 100 % et les nouveaux coûts sont dans
      `data/extracted`, pas encore dans `data/generated`)

## Saeran

- [x] `/admin/editor/characters/2000129` — `role`, `skillPriority`, `videos`,
      reco d'équipement, puis `rank` / `rankPvp`
- [x] `/admin/editor/ee/2000129` — `rank` + `rank10`
- [x] si le build warn sur un `BT_` inédit : le classer dans `effect-families.json`

## Ascension Singularity — les taux de réussite disparaissent

- [x] `EquipmentDetail.tsx` — colonne `%` de `AxStepsCard` et `axRateColor`
      retirées
- [x] `equip.detail.step_rates` → `equip.detail.step_costs`, 5 locales
- [x] guide `gear` — colonne « Réussite », `rateColor` et le bloc « En cas
      d'échec » retirés ; `ascension_stepsDesc` réécrit en 5 langues

## Joint Challenge — Prototype EX-78

- [x] `pnpm datagen:version-boss` → `versions/2026-08/`
- [x] réviser `config.json` / `recommended.json` / `teams.json` / `tips.json`
      depuis `2026-03`, puis `meta.json.updated`

## Singularité — 2 entrées quotidiennes → 1

- [x] `progress-tracker/tasks.ts` — `'dimensional-singularity'` : `maxCount`
      2 → 1, et le commentaire au-dessus
- [x] `timegate-resources.json` — Blue Stardust (`22031`) gagne sa source
      `singularity-daily-run` (20/semaine) ; les 4 autres lignes étaient déjà
      aux valeurs d'après-patch

## Publication

- [ ] `pnpm commit`, DONE.md dans le même commit

---

Écarté : recrutement Demiurge (premium, permanente) · event dungeon et son shop
· packages et Battle Pass (argent réel) · Chat, Tactics League, écran Aventure ·
damage calculator (non publié).
