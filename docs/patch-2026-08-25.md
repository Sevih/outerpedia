# Patch du 2026-08-25

> Supprimer ce fichier dans le commit qui le solde.

## Données

- [ ] `pnpm dev`, puis lire le diff

## Recrutement — refonte de la garantie et du Dimensional Supply

- [ ] guide `banner-mileage` — documenter la garantie : le héros ciblé tombe en
      100 tirages (2 fois maximum sur pickup / seasonal / limited / Dimensional
      Crossroads, 1 fois par héros Demiurge, tous les 100 sans plafond en Custom
      Rate Up), comptée par bannière et remise à zéro à la fin d'une bannière
      limitée. Le mileage, lui, ne bouge pas
- [ ] même guide — la phrase « Unlike pity counters, mileage allows you to… »
      (`labels.ts`) devient fausse : le jeu a maintenant un pity
- [ ] même guide — la refonte Dimensional Supply : 2 % au total, réparti sur le
      gear rate-up pas encore obtenu, sélection une fois par mois non
      modifiable, plus utilisable une fois les 3 obtenus, remise à zéro le 1er.
      **Solde l'item PRIO de [TODO.md](./TODO.md)**, qui attendait cette refonte
- [ ] `pull-simulator` — la garantie à 100 n'est pas modélisée (le simulateur
      ne connaît que le mileage et la garantie 2★ du x10)
- [ ] tag `free` sur les 5 persos qui quittent le pool et deviennent récompense
      de première clear : Pesketh `2000041`, Bleu `2000045`, Guizam `2000049`,
      Flamberge `2000050`, Orox `2000051` — Maxwell `2000028` et Leo `2000042`
      le portent déjà
- [ ] guide `free-heroes-start-banner` — `free-heroes-sources.json` gagne la
      source « première clear de donjon d'histoire » avec ces 7 persos

## World Boss — Ragnakeus

- [ ] `pnpm datagen:version-boss` → `versions/2026-08/` (précédentes : 2024-10,
      2025-12)
- [ ] réviser `config.json` / `recommended.json` / `teams.json` / `tips.json`,
      puis `meta.json.updated`

## Dimensional Singularity — parcours d'amélioration

- [ ] guide `gear` — l'activation ne se fait plus au « Singularity Ascension
      Device » du lobby mais dans l'écran d'amélioration du gear
      (`ascension_*` de `labels.ts`) ; vérifier les autres mentions du lobby

## Terminus Isle — rendement d'un run

- [ ] `daily-stamina` — les récompenses de première clear ajoutées (matériaux
      de cadeau Normal/Supérieur, stages 1 à 5) ne sont nulle part. Le coût
      (30 Stamina/jour) et le nombre de runs, eux, ne changent pas
- [ ] `ether-income` — Sweep All prend maintenant la meilleure récompense au
      choix sur les Event Spots : l'estimation « 22–26 sur la treasure reward
      de Terminus 10 » est à réévaluer

## Publication

- [ ] `pnpm commit`, DONE.md dans le même commit

---

Écarté : event dungeon [Festival and the Secret Recipe] et son shop · Fireworks
· packages (argent réel) · reset de pré-saison Real Time Arena · correctifs
d'UI · la refonte Terminus elle-même (météo ouverte à tous, 30 000 or au lieu
de 150 000, temps d'exploration supprimé, effets de formation revus) : le site
n'a pas de guide Terminus, seul son rendement est documenté — cf. la section
ci-dessus.
