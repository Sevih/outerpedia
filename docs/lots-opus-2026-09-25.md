# Prompts pour agents Opus — tri du TODO au 25/09/2026

> Tri de [TODO.md](./TODO.md), relu contre le code le 25/09 : ce qui est assez
> cadré, autonome et vérifiable (tsc + eslint + vitest) pour partir à un agent
> sans arbitrage en cours de route. Chaque bloc ci-dessous est un PROMPT
> COMPLET à coller tel quel dans une session, préambule compris — le préambule
> répond d'avance aux questions que les quatre premiers lots ont posées
> (entrée DONE, qui commite, quels fichiers, autres fichiers modifiés, tests de
> hook), pour ne plus avoir d'aller-retour. Un prompt par agent ; plusieurs
> agents peuvent tourner en même temps dans la même arborescence tant que
> leurs lots ne touchent pas les mêmes fichiers (B2, B3 et B6 écrivent tous
> les six locales : pas ensemble).
>
> Taille : A = une heure ou moins, B = une demi-journée, C = une journée.

## Faits (25/09, quatre agents en parallèle, un lot chacun)

| Lot                                          | Commit                                         |
| -------------------------------------------- | ---------------------------------------------- |
| A1 · Lefthook : format avant lint            | `chore(hooks): format avant lint, en sequence` |
| A2 · `esc()` / `rfc822()` mutualisés         | `6f07fe04` (amendé avec son entrée DONE)       |
| A3 · `CLASS_ORDER`, ordre de l'enum du jeu   | `eb5aa4bc`                                     |
| A4 · Parents Twitch dérivés des hôtes servis | `d5b5373e`                                     |

Leçons reprises dans le préambule : A2 a commité sans entrée DONE ; A3 s'est
arrêté sur l'ordre des classes (bonne réaction, réponse : l'enum du jeu dans
TextSystem) ; A1 a testé son hook dans un worktree, ce qui a déclenché un
`pnpm install` et redirigé les hooks git le temps du test ; tous ont demandé
qui commite et quoi indexer.

## A5 · Token de taille `--text-2xs`

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
Le dépôt compte environ 350 occurrences de `text-[10px]` et `text-[11px]` dans `src/`. Le système de tokens de `src/app/globals.css` n'a pas de taille en dessous de `text-xs`.

Objectif : (1) compter les occurrences de chaque valeur ; (2) ajouter un token `--text-2xs` selon la mécanique des tokens de taille déjà en place dans `globals.css` (regarde comment `text-xs` ou les autres tailles sont déclarées pour Tailwind v4), à la valeur MAJORITAIRE ; (3) remplacer par script toutes les occurrences de cette valeur par `text-2xs` (attention aux variantes `md:text-[10px]`, `sm:` etc. : elles se remplacent aussi). Laisse l'autre valeur telle quelle et dis combien il en reste.

Zéro changement visuel attendu : ouvre trois pages qui utilisent ces classes (une fiche perso, `/wallpapers`, un outil) avant et après, et confirme.
```

## A6 · Admin : `key={i}` sur des listes réordonnables

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
Dans l'admin, des listes réordonnées par `MoveButtons` utilisent l'index comme clé React : `src/components/admin/editorial/EventsEditor.tsx` (lignes 112 et 384) et `src/components/admin/premium-limited/PremiumLimitedParts.tsx` (ligne 503). Conséquence : l'état interne d'`InlineTextField` reste collé à la POSITION quand on déplace un bloc, le texte en cours d'édition saute d'un bloc à l'autre.

Objectif : une clé STABLE par bloc. Utilise l'identifiant du modèle s'il existe ; sinon pose un id à la création du bloc (`crypto.randomUUID()`), porté par l'objet en mémoire (jamais dérivé de l'index). Ne touche PAS aux `<option key={i}>` d'un `<select>` statique qui n'est pas réordonné (ligne 384 : vérifie de quoi il s'agit avant de décider).

Vérification : décris le scénario manuel (éditer un champ, déplacer le bloc, vérifier que le texte suit) et confirme que le JSON sauvegardé est inchangé (l'id ne doit pas fuir dans la donnée persistée, sauf s'il existe déjà).
```

## A7 · Fichiers JSON écrits hors `formatJson`

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
`datagen/lib/json.ts` définit `formatJson`/`writeJson` (format prettier canonique) et `writeTextAtomic`. Certains écrivains passent à côté : `datagen/video-meta.ts:135` écrit `JSON.stringify(sorted, null, 2) + '\n'` ; vérifie aussi `comics.json` (`datagen/generators/comics.ts`, `datagen/assets/collect-comics.ts`, `sync-comics-seed.ts`) et `posts.json` (cherche qui l'écrit). Un JSON écrit hors format passe prettier « par chance de forme » ; un tableau court le ferait échouer en CI.

Objectif, pour chaque fichier : soit le faire écrire par `writeJson`/`formatJson`, soit, s'il est délibérément hors format, l'ajouter à `.prettierignore` avec un commentaire dans le style de ceux qui y sont déjà (lis le fichier : trois cas y sont documentés, dont `anim-events.json` ajouté le 25/09). Lis le commentaire de `json.ts` autour de `writeTextAtomic` : il liste les fichiers « sous un autre format », respecte les décisions déjà prises.

Régénère chaque fichier une fois pour que la version committée adopte le format final, et vérifie `pnpm format:check`.
```

## A8 · `retired` posé par promote, absent des types

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
`datagen/promote.ts` marque `retired: true` les entités retenues par la rétention (`monsters.json`, `monster-skills.json`, `encounters.json`, via `applyRetention`) et, depuis le 25/09, les entrées retenues des catalogues `wallpapers.json` et `bgm_mapping.json` (`applyCatalogRetention`). Côté types, seul `DungeonRef.retired` existe ; les types de monstre, de skill de monstre et d'encounter ne connaissent pas cette clé.

Objectif : ajouter `retired?: boolean` (avec un commentaire d'une ligne : « posé par la rétention de promote, entité retirée du jeu ») aux types concernés, dans `src/types/*` et dans les interfaces datagen correspondantes. S'il existe un validateur de contrat ou un schéma (cherche `zod`, `validate`, `contract` dans `datagen/` et `src/lib/data`), qu'il accepte la clé. Aucun changement de comportement : aucun fichier de `data/generated` ne doit bouger.
```

## A9 · `scripts/init.ps1` : mauvaise chaîne et promotion sans revue

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
`scripts/init.ps1` (script d'amorçage Windows) teste la présence de LDPlayer mais lance ensuite la chaîne STEAM, puis promeut sans revue avec `datagen:regen`.

Objectif : (1) gater la chaîne Steam sur la détection de Steam — regarde `datagen/extract/steam.ts` (`findSteamInstall`) et `pull-steam.ts` pour la logique de détection à reproduire ou à appeler ; (2) remplacer `datagen:regen` (build + promote --apply aveugle) par `datagen:patch` (le flux avec revue, cf. `datagen/refresh.ts` et `datagen/README.md`). Mets le script en cohérence avec les scripts réellement définis dans `package.json`. Tu ne peux pas l'exécuter ici : relis-le ligne à ligne et explique chaque changement.
```

## A10 · Un `eslint-disable` évitable et des doublons dans les guides

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
(1) `src/app/[lang]/guides/_contents/general-guides/ether-income/Calculator.tsx:217` porte un `eslint-disable-next-line react-hooks/exhaustive-deps` (« amountOf dérive des index sélectionnés »). Restructure pour le retirer : mémoïser `amountOf` avec `useCallback`/`useMemo` sur ses vraies dépendances, ou le passer en dépendance. Le calcul rendu doit rester identique.

(2) Dans `src/app/[lang]/guides/_contents/`, cherche : un composant `QA` doublon de `QACard` ; des composants `Card`, `Heading`, `TableShell` définis DANS le corps d'un composant (re-créés à chaque rendu, perte d'état) — à sortir au niveau module ; `goldCell` défini deux fois. Dédoublonne en gardant l'implémentation la plus complète, sans changer le rendu.
```

## A11 · Filtres de classe et d'élément alignés sur `CLASS_ORDER` / `ELEMENT_ORDER`

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
Le lot A3 (commit eb5aa4bc) a posé `CLASS_ORDER` dans `src/lib/images.ts`, à côté d'`ELEMENT_ORDER` : l'ordre de l'enum du jeu (defender, striker, ranger, mage, healer), et l'a substitué aux copies en dur. Restent des listes de classes et d'éléments construites AILLEURS :

(1) Filtres triés alphabétiquement parce que bâtis depuis les clés des données (glossaire) : `/characters`, la tier list, l'équipement, le team planner, most-used-units. Trouve chaque site (`grep` sur `Object.keys(` des classes, `.sort()`, `classes` du glossaire) et impose l'ordre de `CLASS_ORDER` (et `ELEMENT_ORDER` pour les éléments quand le même site trie aussi les éléments). Les filtres stockent des slugs, pas des positions : aucune donnée persistée ne bouge, vérifie-le quand même sur le localStorage de la tier list et du team planner.

(2) `CLASS_SLUGS` défini deux fois (`portrait-layout.ts`, `parse-text.tsx`) : une seule définition, dérivée de `CLASS_ORDER` si c'est la même liste.

(3) `ELEMENTS` recopié à la main dans hero-tracker et `src/app/dev/portrait/page.dev.tsx` alors qu'`ELEMENT_ORDER` existe : consommer `ELEMENT_ORDER`.

NE TOUCHE PAS aux positions de bits de `filter-codec.ts` (ligne ~69) : elles encodent des liens partagés déjà en circulation, leur ordre est figé.

Vérification : sur `/characters` et la tier list, les pastilles de classe apparaissent dans l'ordre defender, striker, ranger, mage, healer ; typecheck, lint, test.
```

## B1 · `lib/format-duration.ts` : durées localisées

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
Quatre copies d'un formatage de durée en anglais (« 3d 4h 12m ») servent dans toutes les langues : `src/components/home/BannerCountdown.tsx:17`, `src/components/home/BuffEventTimer.tsx:70`, `src/components/home/ServerResets.tsx:41`, `src/app/[lang]/tools/_contents/progress-tracker/tracker.ts:464`. `SingularityCountdown` a tranché autrement (affichage numérique `hh:mm:ss`) : ne le touche pas.

Objectif : `src/lib/format-duration.ts`, une fonction PURE `formatDuration(ms, lang, opts)` qui rend la même forme pour les quatre sites. Deux voies, à évaluer en tête de fichier : `Intl.NumberFormat` avec `style: 'unit'`, `unitDisplay: 'narrow'` (unités `day`, `hour`, `minute`) — vérifie le rendu réel en jp/kr/zh/fr/es ; ou des clés i18n `duration.day/hour/minute` (abréviations) dans les six locales. Choisis la voie dont le rendu est court et lisible dans un compte à rebours, et explique.

Tests unitaires : 0 jour, 1 jour, arrondi des minutes, chaque langue. Remplace ensuite les quatre copies. Vérifie sur la page d'accueil en fr et jp.
```

## B2 · Chaînes UI en dur dans les outils et composants (G24)

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
Le rapport `docs/audit/transverse.md`, constat G24, liste 24 sites dans les outils et 8 dans les composants où du texte est codé en dur au lieu de passer par `t()`. Lis d'abord ce constat : c'est ta liste de travail. Exemples : messages d'erreur EN FRANÇAIS remontés tels quels à l'utilisateur par `src/app/[lang]/tools/_contents/hero-tracker/roster-import.ts:108-113` (à transformer en codes d'erreur que l'appelant traduit) ; « Failed to load track » dans `ost/OstPlayer.tsx:163` ; `fire`/`striker` bruts en `alt`/`title` et dans le picker du hero-tracker ; `ON`/`OFF` ; `aria-label` anglais ; `toLocaleString('en')` à passer sur la langue courante.

Objectif : pour chaque site, une clé i18n dans les six locales, ou l'usage d'un libellé déjà existant (cherche avant de créer : les glossaires du jeu couvrent classes et éléments). Ne touche pas aux guides (`guides/_contents`, autre lot). Dans le résumé, liste chaque site traité avec la clé utilisée, et ce que tu as laissé.
```

## B3 · Anglais en dur et `alt` dans les guides (H7, H8)

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
Constats H7 et H8 de `docs/audit/guides.md` (lis-les d'abord). H7, anglais en dur dans des rendus localisés : « Cost: » (`general-guides/gear/index.tsx:506,515`), `orLabel="or"` et « Lv. 1 » (`core-fusion/index.tsx`), « Lv {lv} » (`fusion.tsx`), « WB: » et `alt="Ether"` (`ether-income`), « SPD » dans `BuildRequirements` et `TurnOrder` (utilise `statAbbr('spd')`, qui existe), `PERIOD_ABBR` D/W/M/O (`shop-purchase-priorities`), `aria-label` « N stars » (`premium.tsx`), cinq `alt` de `roadmap-2026`, `lang="en"` sur le conteneur d'`outerplane-on-linux`. H8, `alt` contraires à la règle maison (règle dans `CONVENTIONS.md`, lis-la) : `HeroReviewCard` (slugs bruts), doublons icône+texte dans `premium.tsx`, `BannerTabs`, `LicenseTabs`, `CardArt`, `fusion.tsx`.

Objectif : clés i18n dans les six locales ou libellés existants (cherche avant de créer) ; `alt` conformes à la règle. Vérifie le rendu de trois guides en fr et jp. Liste chaque site traité dans le résumé.
```

## B4 · Quatre index « nom EN → item », trois avec repli muet (H5)

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
Trois guides recopient une IIFE `CATALOG_BY_NAME` qui, sur un nom d'item inconnu, rend un `<span>` texte sans rien dire : `src/app/[lang]/guides/_contents/general-guides/gear/index.tsx`, `heroes-growth/index.tsx`, `shop-purchase-priorities/index.tsx`. La fonction canonique `itemChipByName(name, lang)` existe dans `src/components/guides/editorial/banner/items.ts:44` et JETTE sur un nom inconnu — c'est le comportement voulu : un item renommé par le jeu doit casser le build, pas s'afficher en texte brut.

Objectif : consommer `itemChipByName` dans les trois guides, supprimer les trois copies. Si un guide passe un nom que `itemChipByName` ne connaît pas, le build échouera : c'est une information, corrige le nom dans le guide (vérifie l'item réel dans `data/generated`) et signale-le. Lance le build des pages concernées ou au minimum les tests des guides.
```

## B5 · 152 couleurs Tailwind brutes dans 17 guides (H6)

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
Les guides utilisent des couleurs Tailwind brutes (`text-sky-400`, `bg-amber-500/10`…) alors que des tokens éditoriaux `--ed-{sky,violet,emerald,amber,rose,cyan}` existent pour ça. Constat H6 de `docs/audit/guides.md` : 152 occurrences dans 17 fichiers, dont `how-to-play` (23), `banner-mileage` (20), `roadmap-2026` (14 + 12 dans son `data.ts`), `outerplane-on-linux` (14), `daily-stamina` (13). Des SVG codés en dur dans `MonadGateMap`, `TowerCombatRoster`, `BannerTabs`, `AdventureGrid`.

Avant de commencer : trouve la définition des tokens `--ed-*` dans `src/app/globals.css` et UN guide qui les utilise déjà correctement, il sert de modèle. Objectif : remplacer chaque couleur brute par le token de la même teinte ; pour les SVG, passer les couleurs en `currentColor` ou en variable CSS. Aucun changement visible en thème clair NI sombre : compare trois guides avant/après dans les deux thèmes et dis ce que tu as comparé. Si une couleur n'a pas de token équivalent, laisse-la et liste-la.
```

## B6 · `alt` et accessibilité des outils (G37, G38)

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
Constats G37 et G38 de `docs/audit/transverse.md` (lis-les, ils portent les lignes exactes) : 18 `alt` contraires à la règle maison (règle dans `CONVENTIONS.md`) ; des `<div onClick>` et des boutons-icône sans nom accessible dans progress-tracker, OST, tier-list-maker, team-planner et les galeries.

Objectif : `alt` conformes ; chaque élément cliquable devient `<button type="button">` (ou `<a>` si c'est une navigation) avec un `aria-label` traduit (six locales) ; les icônes décoratives passent en `aria-hidden`. Pas de changement visuel : garde les classes. Vérifie au clavier (Tab, Entrée) sur deux outils et décris ce que tu as testé.
```

## B7 · `labels.ts` des guides contre les locales (H9)

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
Constat H9 de `docs/audit/guides.md` : 92 chaînes courtes des fichiers `labels.ts` de guides existent déjà dans les locales, deux vocabulaires pour un même mot. Règle à appliquer : `t()` pour le CHROME (boutons, en-têtes de tableau, libellés d'interface, unités), `labels.ts` pour la PROSE éditoriale propre au guide.

Objectif : passer par `t()` ce qui est du chrome et a déjà une clé (ne crée pas de clé pour ce lot : s'il n'y en a pas, c'est de la prose, laisse-la). Supprime les entrées de `labels.ts` devenues inutilisées. Liste dans le résumé les 92 cas en deux colonnes : migré vers quelle clé, ou laissé et pourquoi.
```

## B8 · banner-mileage : hash d'onglets, priorité `reward`, `stamp:guides` automatique (H12, H13, H14)

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
Trois petits points du guide `banner-mileage` et de l'outillage des guides (constats H12–H14 de `docs/audit/guides.md`).

H12 : le hash d'URL des onglets est trompeur, `#banner=pickup` ouvre l'onglet custom. Aligne les valeurs de hash sur les onglets réels, en acceptant encore les anciennes valeurs (liens externes existants).

H13 : la priorité entre `reward` et `rewardWin` est inversée entre trois sites du code (cherche-les). Une seule règle, factorisée ; sans effet visible aujourd'hui, donc un test unitaire qui fige la règle.

H14 : rendre `pnpm stamp:guides` (script `scripts/stamp-guides.ts`, qui re-date les guides) automatique en pre-commit lefthook quand un fichier de `src/app/[lang]/guides/_contents` est indexé, avec `stage_fixed` pour que les `meta` re-datés partent dans le même commit. Vérifie sur un commit de test jetable, puis annule-le.
```

## C1 · Imports directs de `@data/*` hors data layer + règle eslint (G14)

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
`CONVENTIONS.md` interdit d'importer `@data/*` ailleurs que dans la couche d'accès `src/lib/data`. Le constat G14 de `docs/audit/transverse.md` en compte plus de 40 : `skills.json`, `equipment/ee.json`, `glossaries.json` (chargé en statique à un endroit, lu au disque à un autre), six guides, cinq outils. Fais l'inventaire exact par `grep` avant de commencer.

Objectif : (1) un accesseur par fichier dans `src/lib/data`, sur le modèle des accesseurs existants (lis-en deux) ; (2) remplacer chaque import direct ; (3) une règle eslint `no-restricted-imports` sur `@data/*` (et `@data/generated/*`) partout SAUF `src/lib/data/**`, BLOQUANTE, dans `eslint.config.mjs` (regarde comment les règles à liste blanche y sont déjà écrites, par exemple `ADMIN_SHIPS_TO_PROD`). Pour `glossaries.json`, unifie sur un seul mode de chargement via l'accesseur et explique le choix.

C'est un gros lot mécanique : Sevih te lance sur une branche dédiée qu'il crée lui-même (tu ne crées ni branche ni worktree, cf. préambule) ; commite dessus. Le rendu de toutes les pages doit être identique : `pnpm test` plus un `next build` si l'environnement le permet.
```

## C2 · Boutons « Télécharger » qui ouvrent le fichier au lieu de le télécharger (G19)

```text
Tu travailles dans le dépôt `outerpedia` (Next.js + pipeline datagen), à la racine du projet, sur la branche `main`. Avant de toucher au code, lis `CONVENTIONS.md` et `CLAUDE.md` et respecte-les. Ce qui suit répond d'avance aux questions habituelles : ne les repose pas, applique.

Périmètre et méthode
- Périmètre STRICT à la tâche ci-dessous. Pas de refactor à côté, pas de « pendant que j'y suis ». Ce que tu repères hors périmètre va dans ton résumé, pas dans le diff.
- D'autres fichiers modifiés ou non suivis peuvent apparaître dans le working tree pendant ta session : ce sont d'autres lots qui tournent en parallèle. Ne les touche pas, ne les indexe pas, ne les signale pas, c'est attendu.
- Si la tâche cache une décision (métier, comportement ambigu, ordre de référence inconnu), arrête-toi AVANT de modifier quoi que ce soit et pose UNE question précise avec ta recommandation. Ne tranche pas toi-même.
- Toute clé i18n ajoutée l'est dans les SIX locales `src/i18n/locales/{en,fr,es,jp,kr,zh}.ts` (`keys.test.ts` exige la parité). Cherche une clé existante avant d'en créer une. Pour jp/kr/zh, traduction sobre ; en cas de doute, l'anglais, signalé dans le résumé.
- Ne lance jamais `pnpm dev`, `pnpm install`, `lefthook install`, ni un `git worktree add`. Si un serveur écoute déjà sur :3000, tu peux le lire avec `curl`. Si tu dois tester un hook git, fais-le sur une branche jetable DANS cette arborescence, en n'indexant que ton fichier de test, puis reviens sur `main` et supprime la branche.

Vérification (obligatoire avant de rendre)
- `pnpm typecheck`, `pnpm lint`, `pnpm test` doivent passer. Cite la dernière ligne de chacun dans ton résumé.
- Aucun changement visuel non demandé ; quand la tâche en interdit, dis ce que tu as comparé.

Livraison (fais-la toi-même, sans demander)
1. Entrée dans `docs/DONE.md`, en TÊTE de la section du jour (`## 2026-MM-JJ`, crée-la si elle manque, sous le préambule du fichier) : une puce, titre en gras, puis le quoi, le pourquoi, le comment, la vérification, et ce que tu as laissé — dans le style des entrées voisines (français, prose dense, noms de fichiers en backticks).
2. Si l'item existe dans `docs/TODO.md`, retire-le (ou marque « FAIT » et ce qui reste, si la ligne porte d'autres points).
3. Commit par toi, en indexant EXPLICITEMENT et uniquement tes fichiers plus `docs/DONE.md` (et `docs/TODO.md` si touché). Jamais `git add -A`, jamais `git add .`. Message en français, préfixe conventionnel (`fix(...)`, `refactor(...)`, `chore(...)`, `feat(...)`), une ligne, sans accents. Les hooks lefthook formatent et lintent ce qui est indexé : si le lint bloque, corrige et recommite. Pas de `--amend` sur un commit qui n'est pas le tien.
4. Pas de push.
5. Rends : le hash du commit, puis un résumé de 5 à 10 lignes (fait, pourquoi, vérifié comment, laissé et pourquoi).

Tâche :
Les liens `<a download>` des wallpapers et de l'OST pointent cross-origin vers R2 (`img.outerpedia.com`) ; sans en-tête `Content-Disposition`, le navigateur ignore `download` et OUVRE le fichier. Vérifié le 25/09 : `curl -I https://img.outerpedia.com/images/download/Cutin/T_CutIn_2000001.png` ne renvoie pas de `Content-Disposition`.

Deux voies, la première est préférée si elle tient :
(1) `scripts/assets-push.mjs` pose déjà des en-têtes S3 à l'upload (`--header-upload`, ligne ~204, Cache-Control). Ajouter `Content-Disposition: attachment` pour les clés `audio/bgm/*` et les fichiers de TÉLÉCHARGEMENT sous `images/download/*`. Attention : les `.webp` de `images/download/*` servent aussi à l'AFFICHAGE (`<img src>`), et les full-arts archivés `images/download/HeroFullArt/*@n.webp` sont téléchargés en webp (cf. `src/lib/wallpapers.ts`). Vérifie si `attachment` casse un `<img>` (normalement non, mais teste) ; si oui, ne le poser que sur `.png` et `.mp3`. Un en-tête S3 étant figé à l'upload, il faut un re-push : ajoute au script une option ciblée (préfixe) plutôt qu'un `--full` global, et documente-la dans l'en-tête du script. NE LANCE PAS le push toi-même : prépare le changement et décris la commande à lancer.
(2) Sinon, côté client : `fetch → blob → a[download]` avec un repli sur l'ouverture simple.

Documente la voie retenue dans le script ou le composant, et dans `datagen/README.md` s'il décrit le push.
```

## Reste à Sevih (jugement, décision ou test physique)

- Hero-tracker : boucle de revue (axes, barème Core Fusion) — arbitrage.
- Découpe des gros composants — RÉSERVÉ SEVIH (décision 25/08).
- SEO : descriptions d'outils — arbitrage sous-titre visible vs meta.
- Pull simulator hors-focus (G9) — bloqué données, et domaine.
- Chips au toucher (G4) — test sur téléphone.
- Tours very hard G10, scaling G11, dry-run damage G12 — sémantique du jeu ou
  du pipeline, une erreur passe les tests.
- `pnpm commit` et `git add -A` (G17) — touche le flux de publication.
- `China_Traditional` — décision produit.
- Lot d'audit `portrait-fx-*.ts` — c'est un audit, pas un correctif.
