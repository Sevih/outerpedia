import type { MonadNode, MonadEdge } from "@/types/monad";

export const routeTitle = "Deeps 6 – Route 3";

export const nodes: MonadNode[] = [
    { id: "A", x: 0, y: 0, type: "start", truePath: true }
];

export const edges: MonadEdge[] = [
    
];
