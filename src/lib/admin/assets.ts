/**
 * Assets visuels de l'admin — ré-exporte le module partagé `@/lib/images`.
 * (Conservé pour ne pas casser les imports admin existants.)
 */
export { img, ELEMENT_TEXT, CHAIN_PILL, ROLE_PILL } from '@/lib/images';
