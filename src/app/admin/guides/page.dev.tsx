export const dynamic = 'force-dynamic';

export default function GuidesIndex() {
  return (
    <div className="space-y-3">
      <h1 className="text-content-strong text-xl font-semibold">Guide editor · Joint Challenge</h1>
      <p className="text-content-muted text-sm">
        Choisis un guide à gauche pour l’éditer. Pilote : <strong>joint-challenge</strong> (monstre
        + conseils + persos + équipe + vidéos, par version). Les autres catégories de la famille
        arriveront ensuite.
      </p>
    </div>
  );
}
