# pnpm configuration

# Enable build scripts (pnpm 10.18+ requires explicit approval)
# This allows native compilation for packages like sharp, esbuild, @tailwindcss/oxide
enable-pre-post-scripts=true
